kind: Service
apiVersion: v1
metadata:
  name: service-c
  labels:
    app: service-c
    visualize: "true"
spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: service-c
    serving: "true"
