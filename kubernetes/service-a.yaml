kind: Service
apiVersion: v1
metadata:
  name: service-a
  labels:
    app: service-a
    visualize: "true"
spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: service-a
    serving: "true"
